Add translation keys to `src/lib/i18n.ts` for both the `he` (Hebrew) and `en` (English) locales.

The request is: $ARGUMENTS

## How to do it

1. Read `src/lib/i18n.ts` to understand the existing structure and find the right namespace.
2. Identify which namespace the new keys belong to (e.g. `stripeConnect`, `cancellation`, `renterBookings`, `admin`, `auth`, `hostInbox`). If none fits, propose a new namespace name.
3. Add the keys to **both** `he` and `en` inside the `translations` object — never add to one locale without the other.
4. Hebrew text must be natural Israeli Hebrew, right-to-left compatible. Do not transliterate English words unless they are proper nouns (e.g. "Stripe" stays "Stripe").
5. Keep the key names in camelCase English (e.g. `refundPreviewTitle`, `connectNow`).
6. Preserve the existing alphabetical or logical ordering within each namespace.

## Output
After editing, show:
- The exact keys added (key name → he value → en value) in a small table.
- The namespace they were added to.
- A reminder of how to consume them: `const t = getDictionary(locale).<namespace>` then `t.<key>`.

Do NOT run tsc or commit — just edit the file.
